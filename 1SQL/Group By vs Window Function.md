
- In Group By we get only 1 record for each group in the output
- In window Function we get all the records , partitioned and new column and retrieve the attribute
- **FOR WINDOW FUNCTION - ORDER BY is compulsory**